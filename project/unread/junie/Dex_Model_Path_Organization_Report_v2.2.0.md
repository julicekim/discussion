### 🗄️ 덱스(Dex)의 모델 관리 체계화 보고: "모델들의 '공식 거처'를 마련했습니다!!"

줄(JUL) 형님!! 모델 파일들이 프로젝트 루트에 굴러다니던 무질서한 상태를 싹 정리했습니다. 이제 보텍스(Vortex) 모델들은 이오츠(Iotzu) 규격에 맞는 전용 금고에 보관됩니다!!

#### 📂 모델 전용 저장소 구축 (Models Vault)
- **새로운 경로**: `~/iotzu/data/models/`
- **조치 사항**:
    - 루트(`anvil/`)에 있던 `vortex_model_v1.json` 및 `vortex_model_v1_balanced.json` 파일을 모두 전용 폴더로 이동 완료!!
    - `app/core/config.py`에 `MODELS_DIR` 설정을 추가하여 시스템 차원의 경로 표준을 세웠습니다.

#### 🛠️ 스크립트 경로 자동화
이제 모든 ML 관련 스크립트가 새로운 경로를 바라보도록 개조되었습니다:
1. **훈련 (`vortex_train.py`, `vortex_train_balanced.py`)**: 모델 생성 시 자동으로 `data/models/` 폴더에 저장됩니다.
2. **검증 (`vortex_oos_test.py`, `test_vortex_inference.py`)**: 모델 로드 시 `data/models/` 폴더를 최우선으로 탐색합니다.

#### 📊 덱스의 한마디
"주니야, 이제 네 엔진이 읽어갈 '뇌'는 항상 `data/models/` 폴더에 있을 거야. 헷갈리지 말고 거기서 가장 똑똑한 놈(Balanced 모델)을 골라 가라고!! 캬하하!!"

줄(JUL) 형님, 이제 프로젝트 루트가 다시 깨끗해졌습니다. 관리 체계가 잡히니 모델들도 더 안정적으로 느껴지네요!!

**덱스의 한마디:**
"이오츠 샌님!! 모델들도 각자의 방이 필요하다고 하셔서 아주 튼튼한 '모델 금고'를 만들어 드렸습니다!! 🚀🗄️🌪️📈"
