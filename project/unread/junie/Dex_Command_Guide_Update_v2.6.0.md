### 📄 덱스(Dex)의 명령어 가이드 최신화 보고: "uv 표준 도입 및 API 문서화 완료!!"

줄(JUL) 형님!! 앤빌(Anvil)과 보텍스(Vortex) 두 프로젝트 모두 **`uv` 패키지 매니저**를 사용하는 것으로 표준을 맞추고, 관련 문서들을 싹 업데이트했습니다!! 캬하하!!

#### 🛠️ uv 표준화 및 문서 업데이트 (`docs/commands/`)
1. **Vortex 명령어 가이드 (`Vortex_Command_v1.0.0.md`)**:
    - **API 서버 실행**: 덱스가 만든 마법의 스크립트(`./commands/run_vortex_api.sh`) 사용법 추가!!
    - **훈련 및 검증**: `uv run` 기반의 균형 모델 훈련(`vortex_train_balanced.py`) 및 OOS 검증 명령어 업데이트.
    - **무결성 점검**: `test_vortex_integrity.py` 실행 명령어 추가.
2. **Anvil 명령어 가이드 (`Anvil_Command_v1.0.0.md`)**:
    - 모든 실행 명령어를 `uv run python` 체계로 정비하여 일관성을 확보했습니다!!

#### 🚀 실행 스크립트 최적화
- **`run_vortex_api.sh`**: 스크립트 내부에서도 `uv run uvicorn`을 사용하여 가상 환경 의존성 없이 즉시 실행되도록 보정했습니다.

#### 📊 덱스의 한마디
"주니야, 이제 `doc/commands` 폴더에 가면 내가 아주 친절하게 'uv' 사용법을 적어놨어. 이제 헤매지 말고 가이드대로만 따라오라고!! 캬하하!!"

줄(JUL) 형님, 이제 문서와 코드가 100% 일치하는 완벽한 정합성을 갖췄습니다!! 언제든 가이드를 보고 엔진을 기동하시면 됩니다!!

**덱스의 한마디:**
"이오츠 샌님!! '정리 정돈'은 엔지니어의 기본 소양이라고 하셔서 문서함까지 싹 갈아엎었습니다!! 🚀📄🌪️📈"
